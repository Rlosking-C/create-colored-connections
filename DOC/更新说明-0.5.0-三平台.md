# Create: Colored Connections 0.5.0 更新说明（三平台）

> 用途：0.5.0 的**版本更新说明**，对应 Modrinth 的 Version 页面、CurseForge 的文件 Changelog、MC 百科的更新记录，三处内容一致、仅语言不同。
> 写法：只陈述改动本身，逐条可核对；不写实现原理、不写理由、不写营销语。
> 项目描述不在本次范围内，各平台现有描述页面保持不变。

---

## 一、Modrinth（Version 0.5.0 · Changelog）

```markdown
### Compatibility

- Create 6.0.10 is now the minimum supported version.
- Create: Factory Controller remains optional: without it, or below 1.2.1, the integration stays off.

### Configuration

- Config split into `create_colored_connections-common.toml` (server) and `create_colored_connections-client.toml` (client), so in-game config editors list both. Existing files migrate automatically.
- Config option names and descriptions are now translated.

### Dyeing

- Dyeing uses the vanilla use action. It does not open the gauge panel and does not place a block.
- Path dyeing follows Create's connection controls: Shift+right-click cancels, right-click on a gauge appends, right-click on the start gauge or on any non-gauge cancels.
- A pending path shows an action-bar hint and a blinking outline on the gauge the next click would confirm.
- A gauge that cannot join reports the reason in red, with Create's deny sound.
- Create's gauge hover tip is hidden while a path is pending.

### Rendering

- Preview beams are 1 px wide and no longer overlap; crossings and gauge turns render as clean lines.
- Dyed right-angle corners no longer show a stepped patch over the vanilla line.
- The dye border samples the line texture at 1:1, matching the vanilla line's pattern density.

### Languages

- Added Russian, German, French, Japanese, Korean, Brazilian Portuguese and European Spanish.
- Terminology follows Create's translations and vanilla's dye and colour names.
- Fixed the goggles trace readout detaching from its item icon in longer languages.
- The action-bar path hint is shortened to fit every language.
```

---

## 二、CurseForge（0.5.0 文件 Changelog）

```markdown
Requires Create 6.0.10 or higher. Minecraft 1.21.1, NeoForge 21.1.x.

### Compatibility

- Create 6.0.10 is now the minimum supported version.
- Create: Factory Controller remains optional: without it, or below 1.2.1, the integration stays off.

### Configuration

- Config split into `create_colored_connections-common.toml` (server) and `create_colored_connections-client.toml` (client); existing files migrate automatically.
- Config option names and descriptions are now translated.

### Dyeing

- Dyeing uses the vanilla use action. It does not open the gauge panel and does not place a block.
- Path dyeing follows Create's connection controls: Shift+right-click cancels, right-click on a gauge appends, right-click on the start gauge or on any non-gauge cancels.
- A pending path shows an action-bar hint and a blinking outline on the gauge the next click would confirm.
- A gauge that cannot join reports the reason in red, with Create's deny sound.
- Create's gauge hover tip is hidden while a path is pending.

### Rendering

- Preview beams are 1 px wide and no longer overlap; crossings and gauge turns render as clean lines.
- Dyed right-angle corners no longer show a stepped patch over the vanilla line.
- The dye border samples the line texture at 1:1, matching the vanilla line's pattern density.

### Languages

- Added Russian, German, French, Japanese, Korean, Brazilian Portuguese and European Spanish.
- Fixed the goggles trace readout detaching from its item icon in longer languages.
- The action-bar path hint is shortened to fit every language.
```

---

## 三、MC 百科（0.5.0 更新说明）

```markdown
### 兼容与依赖

- 最低支持版本提升为 Create 6.0.10。
- Create: Factory Controller 保持可选：未安装或版本低于 1.2.1 时集成自动关闭。

### 新增

- 配置拆分为服务端文件 `create_colored_connections-common.toml` 与客户端文件 `create_colored_connections-client.toml`，游戏内配置界面可同时识别，旧配置文件自动迁移。
- 新增俄语、德语、法语、日语、韩语、巴西葡萄牙语、欧洲西班牙语共 7 种语言。

### 调整

- 染色改用原版使用动作；不会打开仪表面板，也不会放置方块。
- 路径染色对齐 Create 的连线操作：Shift+右键取消，右键仪表接入，右键起始仪表或非仪表位置取消。
- 选路期间显示操作栏提示，并以闪烁边框标出下一次点击将要确认的仪表。
- 无法接入的仪表以红色说明原因，同时播放 Create 的拒绝音效。
- 选路期间隐藏 Create 的仪表悬停提示。
- 预览光束宽度改为 1 像素且不再重叠；交叉处与仪表转弯处均为干净线条。
- 活动线路的直角转弯处不再出现深色块。
- 染料描边按 1:1 采样贴图，花纹密度与本体线条一致。
- 操作栏提示文案精简，在全部支持语言下均不超出屏幕。
- 配置项名称与说明随游戏语言翻译。

### 修复

- 修复护目镜读数在较长语言下脱离物品图标、单行中途折行的问题。
```

---

## 附：发布口径

1. 译名统一：工厂仪表 / 连线 / 护目镜 / 染料描边 / 路径染色；英文对应 Factory Gauge / link / Engineer's Goggles / dye border / path dyeing。
2. 每条只写"改了什么"，可对照 `CHANGELOG.md` 的 `## 0.5.0` 段逐条核验；不解释实现，不使用形容词。
3. 不预告后续版本的内容与时间。
